import { createApp } from 'vue';
import App from './App.vue';

// Inicializar la aplicación Vue
const app = createApp(App);

// Montar la aplicación en el elemento con el id 'app' en tu archivo HTML
app.mount('#app');
